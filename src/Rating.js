import React, {Component} from "react";
import '../node_modules/font-awesome/css/font-awesome.css';

export default class Rating extends Component{
    drawStart(ctx, cx, cy, spikes, outerRadius, innerRadius, color){
        var r=Math.PI/(2*3);
        var x=cx;
        var y=cy;
        var step=Math.PI
    }
}